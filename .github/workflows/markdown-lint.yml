---
name: YAML Lint
on:
    push:

jobs:
    linting:
        name: "Lint"
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v2
            name: Check out code
          - name: Lint Code
            uses: docker://avtodev/markdown-lint:v1
            with:
                args: "**/*.md"