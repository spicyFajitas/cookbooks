services:
  # plex is deployed manually on fedora server so as to use hardware transcoding
  plex:
    image: lscr.io/linuxserver/plex:latest
    container_name: plex
    environment:
      - PUID=1002
      - PGID=1002
      - TZ=Etc/UTC
      - VERSION=public
    devices:
      - /dev/dri:/dev/dri
    network_mode: bridge
    ports:
      - '32400:32400'
    volumes:
      - /opt/media/plex/transcode:/transcode
      - /opt/media/plex/config:/config
      - /mnt/media/tv_shows:/tv
      - /mnt/media/movies1:/movies
    restart: always

  {# emby:
    image: lscr.io/linuxserver/emby:latest
    container_name: emby
    environment:
      - PUID=1002
      - PGID=1002
      - TZ=Etc/UTC
    volumes:
      - /opt/media/emby:/config:z
      - /mnt/media/tv_shows:/tv:z
      - /mnt/media/movies1:/movies:z
    ports:
      - 8096:8096
      - 8920:8920 #optional
    devices:
      - /dev/dri:/dev/dri #optional
    restart: always #}
