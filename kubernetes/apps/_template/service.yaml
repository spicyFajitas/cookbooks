apiVersion: v1
kind: Service
metadata:
  name: APP_NAME
spec:
  type: ClusterIP
  selector: {}
  ports:
    - name: web
      port: 8080 # REPLACE_ME: match deployment.yaml's containerPort
      targetPort: web
